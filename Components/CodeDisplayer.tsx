import { Text } from "@nextui-org/react";
import { Layout } from "../Components/UI/Layout";

const CodeDisplayer: React.FC = () => {
  return (
    <Layout css={{ padding: "$6 $8" }}>
      <Text>Hello World</Text>
    </Layout>
  );
};

export default CodeDisplayer;
