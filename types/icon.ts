import { NormalColors, NormalSizes } from "@nextui-org/react";

export default interface NextIcon {
  fill?: NormalColors;
  filled?: boolean;
  size?: NormalSizes;
  height?: number;
  width?: number;
  label?: number;
}